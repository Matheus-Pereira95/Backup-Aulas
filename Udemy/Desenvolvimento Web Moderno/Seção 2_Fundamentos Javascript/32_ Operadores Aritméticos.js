// Operadores aritmeticos -> Operacao binaria ( opera com dois operandos )

const [a, b, c, d] = [8, 6, 2, 50]

const soma = a + b + c + d
const subtracao = d-a
const multiplicacao = a * b
const divisao = b / c
const modulo = a % 2 // Modulo é o resto da soma, por exemplo, numero impar/2 tem resto 1 ( modulo é 1), numero par/2 tem resto 0 (modulo é 0)

console.log(soma,subtracao,divisao,multiplicacao,modulo)